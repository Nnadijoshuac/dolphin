/**
 * Types `window.ethereum` (EIP-1193) for the injected wallet connector in
 * src/wallet/wallet-provider.tsx. viem ships this declaration; referencing it
 * here rather than hand-declaring `any` keeps the provider properly typed.
 */
/// <reference types="viem/window" />
